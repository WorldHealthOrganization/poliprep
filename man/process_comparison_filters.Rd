% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_ona.R
\name{process_comparison_filters}
\alias{process_comparison_filters}
\title{Process and Convert R Filters to MongoDB Query Format}
\usage{
process_comparison_filters(...)
}
\arguments{
\item{...}{One or more filter expressions. Can be formulas (e.g.,
~field > value), vectors, or single values}
}
\value{
A JSON string containing the MongoDB query filters
}
\description{
Converts R filtering expressions into MongoDB query format and returns a
JSON string. Handles single conditions, multiple conditions combined with &,
and vector filters.
}
\details{
Supports the following R operators that are mapped to MongoDB operators:
\itemize{
\item > maps to $gt
\item >= maps to $gte
\item < maps to $lt
\item <= maps to $lte
\item == maps to $eq
\item != maps to $ne
}
}
\examples{
# Single condition
# process_comparison_filters(~age >= 21)

# Multiple conditions
# process_comparison_filters(~age >= 21 & age <= 65)

# Vector filter
# process_comparison_filters(status = c("active", "pending"))
}
